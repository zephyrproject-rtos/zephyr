# This is a version-controlled application-wide configuration.

# Configuration can be done globally to all applications here or on a
# per-application basis. Application-specific configuration takes
# precedence over this configuration.

# Application-wide configuration can also be done by the optional
# gitignored configuration file
# $ZEPHYR_BASE/local-defaults.cmake. local-defaults.cmake has
# precedence over this configuration.

set(BOARD qemu_x86)
