description: Nordic FICR (Factory Information Configuration Registers)

compatible: "nordic,nrf-ficr"

include: base.yaml

properties:
  reg:
    required: true

  "#nordic,ficr-cells":
    type: int
    required: true
    const: 1

nordic,ficr-cells:
  - offset
