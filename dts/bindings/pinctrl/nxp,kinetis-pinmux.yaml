description: Kinetis pinmux node

compatible: "nxp,kinetis-pinmux"

include: base.yaml

properties:
    reg:
      required: true

    clocks:
      required: true

child-binding:
    description: bar
    properties:
      pin:
        required: true
        type: int

      mux:
        required: true
        type: int
