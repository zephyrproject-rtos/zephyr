title: RV32M1 Pinmux

description: |
    This is a representation of the RV32M1 Pinmux node

compatible: "openisa,rv32m1-pinmux"

include: base.yaml

properties:
    reg:
      required: true

    clocks:
      required: true

pinmux-cells:
  - pin
  - function
