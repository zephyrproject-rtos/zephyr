description: Bluetooth HCI on Silabs boards

compatible: "silabs,bt-hci-efr32"

include: bt-hci.yaml

properties:
  bt-hci-name:
    default: "efr32"
  bt-hci-bus:
    default: "virtual"
  bt-hci-quirks:
    default: ["no-reset"]
