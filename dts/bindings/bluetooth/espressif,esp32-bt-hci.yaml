description: Bluetooth HCI for Espressif ESP32

compatible: "espressif,esp32-bt-hci"

include: bt-hci.yaml

properties:
  bt-hci-name:
    default: "BT ESP32"
  bt-hci-bus:
    default: "ipc"
  bt-hci-quirks:
    default: ["no-auto-dle"]
