description: Bluetooth HCI on Silabs boards

compatible: "silabs,bt-hci"

include: bt-hci.yaml

properties:
  bt-hci-name:
    default: "sl:bt"
  bt-hci-bus:
    default: "virtual"
  bt-hci-quirks:
    default: ["no-reset"]
