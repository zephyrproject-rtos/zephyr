description: Bluetooth HCI on Silabs boards

compatible: "silabs,siwx91x-bt-hci"

include: bt-hci.yaml

properties:
  bt-hci-name:
    default: "sl:bt:siwx91x"
  bt-hci-bus:
    default: "virtual"
  bt-hci-quirks:
    default: ["no-reset"]
