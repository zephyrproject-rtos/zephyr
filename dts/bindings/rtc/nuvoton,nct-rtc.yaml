# Copyright (c) 2025 Nuvoton Technology Corporation.
# SPDX-License-Identifier: Apache-2.0

description: Nuvoton, NCT-RTC node

compatible: "nuvoton,nct-rtc"

include: [base.yaml]

properties:
  reg:
    required: false
  alarms_count:
    type: int
    required: false
  rtc-wk:
    type: phandle
    required: true
    description: |
        Mapping table between Wake-Up Input (WUI) and rtc alarm signal.
        For example, the WUI mapping on NCT6 would be
           rtc-wk = <&wui_rtc>;
