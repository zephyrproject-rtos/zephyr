description: Atmel SAM0 watchdog

compatible: "atmel,sam0-watchdog"

include: base.yaml

properties:
    reg:
      required: true

    label:
      required: true

    interrupts:
      required: true
