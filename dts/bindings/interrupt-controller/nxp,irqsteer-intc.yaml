description: i.MX DSP interrupt controller

compatible: "nxp,irqsteer-intc"

include: [base.yaml]

properties:
  reg:
    required: true

  nxp,version:
    type: int
    description: |
      MCUX SDK IRQSTEER driver version.
