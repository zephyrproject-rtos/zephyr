description: NXP FlexIO SPI

compatible: "nxp,flexio-spi"

include: ["spi-controller.yaml", "pinctrl-device.yaml"]

properties:
  reg:
    required: true

  clocks:
    required: true

  interrupts:
    required: true