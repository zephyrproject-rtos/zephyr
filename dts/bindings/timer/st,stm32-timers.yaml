title: STM32 TIMERS

description: >
    This binding gives a base representation of the STM32 TIMERS

inherits:
    !include base.yaml

properties:
    compatible:
      constraint: "st,stm32-timers"

    label:
      required: true

    reg:
      required: true

    clocks:
      required: true
