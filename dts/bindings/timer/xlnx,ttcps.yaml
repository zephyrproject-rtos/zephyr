description: Xilinx ZynqMP PS TTC timer

compatible: "cdns,ttc"

include: base.yaml

properties:
    label:
      required: true

    reg:
      required: true

    clock-frequency:
      type: int
      required: false
      description: Clock frequency information for Timer operation
