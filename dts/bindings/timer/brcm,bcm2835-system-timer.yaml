description: Broadcom BCM2835 system timer

compatible: "brcm,bcm2835-system-timer"

include: base.yaml

properties:
  reg:
    type: array
    required: true

  interrupts:
    type: array
    required: true

  clock-frequency:
    type: int
    required: true
    description: System timer counter frequency in Hz
