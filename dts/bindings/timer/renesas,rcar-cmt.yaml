description: Renesas R-Car CMT timer

compatible: "renesas,rcar-cmt"

include: base.yaml

properties:
    reg:
      required: true

    clock-frequency:
      type: int
      required: true
      description: Clock frequency information for Timer operation

    clocks:
      required: true

    interrupts:
      required: true
