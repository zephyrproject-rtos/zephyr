description: Test serial port

compatible: "vnd,serial"

include: uart-controller.yaml

properties:
    reg:
      required: true

    interrupts:
      required: true

    baud-rate:
      type: int
      required: false
