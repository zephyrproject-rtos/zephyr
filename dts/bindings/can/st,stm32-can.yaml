title: STM32 CAN

description: >
    This binding gives a base representation of the STM32 CAN controller

inherits:
    !include can.yaml

properties:
    compatible:
      constraint: "st,stm32-can"

    reg:
      category: required

    interrupts:
      category: required

    clocks:
      category: required
