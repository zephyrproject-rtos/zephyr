title: Atmel DMAC binding

description: |
    Binding for the Atmel SAM0 DMA controller.

compatible: "atmel,sam0-dmac"

include: base.yaml

properties:
    reg:
      required: true

    interrupts:
      required: true
