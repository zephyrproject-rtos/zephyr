
title: Atmel DMAC binding
version: 0.1

description: >
    Binding for the Atmel SAM0 DMA controller.

inherits:
    !include base.yaml

properties:
    compatible:
      constraint: "atmel,sam0-dmac"

    reg:
      category: required

    interrupts:
      category: required
