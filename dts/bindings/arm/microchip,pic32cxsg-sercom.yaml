description: Microchip PIC32CXSG multi-protocol (UART, SPI, I2C) SERCOM unit

compatible: "atmel,sam0-sercom"

include: base.yaml

properties:
  reg:
    required: true

  interrupts:
    required: true

  clocks:
    required: true
