title: OpenISA GPIO

description: >
    This is a representation of the OpenISA GPIO nodes

inherits:
    !include base.yaml

properties:
    compatible:
      constraint: "openisa,rv32m1-gpio"

    reg:
      required: true

    interrupts:
      required: true

    label:
      required: true

"#cells":
  - pin
  - flags
