description: Series 0-2 GPIO (General Purpose Input/Output) Peripheral

compatible: "silabs,gecko-gpio"

include: base.yaml

properties:
  reg:
    required: true

  interrupts:
    required: true

  location-swo:
    type: int
    description: Serial Wire Output (SWO) PIN location
