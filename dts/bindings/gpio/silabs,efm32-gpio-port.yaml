
title: EFM32 GPIO
version: 0.1

description: >
    This is a representation of the EFM32 GPIO Port nodes

inherits:
    !include base.yaml

properties:
    compatible:
      constraint: "silabs,efm32-gpio-port"

    reg:
      category: required

    label:
      category: required

"#cells":
  - pin
  - flags
