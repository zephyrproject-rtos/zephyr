title: EFM32 GPIO

description: >
    This is a representation of the EFM32 GPIO Port nodes

inherits:
    !include base.yaml

properties:
    compatible:
      constraint: "silabs,efm32-gpio-port"

    reg:
      required: true

    label:
      required: true

"#cells":
  - pin
  - flags
