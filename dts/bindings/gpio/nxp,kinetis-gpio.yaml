title: Kinetis GPIO

description: >
    This is a representation of the Kinetis GPIO nodes

inherits:
    !include base.yaml

properties:
    compatible:
      constraint: "nxp,kinetis-gpio"

    reg:
      required: true

    label:
      required: true

"#cells":
  - pin
  - flags
