# Copyright 2024 NXP
# SPDX-License-Identifier: Apache-2.0

description: PCA series I2C-based GPIO expander

include: [gpio-controller.yaml, i2c-device.yaml]

# Currently supported device:
#   pca9538    pca9539
#   pca9554    pca9555
#   pca6408    pca6416
#   pca9574    pca9575
#   pcal9538   pcal9539
#   pcal6408   pcal6416
#   pcal6524   pcal6534
#
#   PCA9538   compatible with  PCA9534   PCA9554   PCA6408(*a)
#   PCA9539   compatible with  PCA9535   PCA9555   PCA6416(*a)
#   PCAL9538  compatible with  PCAL9554  PCAL6408(*a)
#   PCAL9539  compatible with  PCAL9555  PCAL6416(*a)
#   (a) PCA(L)64xxA series support voltage level translation,
#       but has no difference from driver's view.
#   (b) PCAL6534 has 34 GPIO pins in 4x 8-bit port and 1x
#       2-bit port. This driver only support 4x 8-bit port.

properties:
  reset-gpios:
    type: phandle-array
    description:
      Reset GPIO pin (active-low)
      Left blank if the device does not have reset pin
      or the pin is not connected in your application.

  int-gpios:
    type: phandle-array
    description:
      Interrupt GPIO pin (active-low open-drain)
      Left blank if the pin is not connected in your
      application.

  "#gpio-cells":
    const: 2

gpio-cells:
  - pin
  - flags
