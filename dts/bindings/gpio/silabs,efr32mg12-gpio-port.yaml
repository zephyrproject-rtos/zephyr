title: EFR32MG GPIO

description: >
    This is a representation of the EFR32MG GPIO Port nodes

inherits:
    !include base.yaml

properties:
    compatible:
      constraint: "silabs,efr32mg-gpio-port"

    reg:
      required: true

    label:
      required: true

"#cells":
  - pin
  - flags
