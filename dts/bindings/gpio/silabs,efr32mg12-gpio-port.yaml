
title: EFR32MG GPIO
version: 0.1

description: >
    This is a representation of the EFR32MG GPIO Port nodes

inherits:
    !include base.yaml

properties:
    compatible:
      constraint: "silabs,efr32mg-gpio-port"

    reg:
      category: required

    label:
      category: required

"#cells":
  - pin
  - flags
