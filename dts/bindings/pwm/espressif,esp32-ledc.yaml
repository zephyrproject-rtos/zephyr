description: ESP32 LEDC

compatible: "espressif,esp32-ledc"

include: [pwm-controller.yaml, base.yaml]

properties:
    label:
      required: true

    "#pwm-cells":
      const: 3

pwm-cells:
  - channel
  - period
  - flags
