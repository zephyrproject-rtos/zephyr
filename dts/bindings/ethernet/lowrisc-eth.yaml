
description: Low Risc Ethernet controller

compatible: "lowrisc-eth"

include: base.yaml

properties:
    reg:
      required: true

    interrupts:
      required: true

    local-mac-address:
      type: uint8-array
      required: false
      description: Specifies the MAC address that was assigned to the network device 

    label:
      required: true

