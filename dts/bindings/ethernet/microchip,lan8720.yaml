# Copyright 2024 Muhammad Waleed Badar
# SPDX-License-Identifier: Apache-2.0

description: Microchip LAN8720 Ethernet PHY device

compatible: "microchip,lan8720"

include: ethernet-phy.yaml

properties:
  reset-gpios:
    type: phandle-array
    description: GPIO connected to PHY reset signal pin. Reset is active low.
  int-gpios:
    type: phandle-array
    description: GPIO for interrupt signal indicating PHY state change.
