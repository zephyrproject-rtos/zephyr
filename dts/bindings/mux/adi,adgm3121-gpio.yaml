# SPDX-FileCopyrightText: 2026 NXP
# SPDX-FileCopyrightText: 2026 Analog Devices Inc.
# SPDX-License-Identifier: Apache-2.0

description: |
  Analog Devices ADGM3121 DPDT MEMS switch (DC-24GHz), parallel GPIO control
  mode, exposed as a MUX controller.

  Each of the four MEMS switches (SW1..SW4) is controlled by a dedicated GPIO
  pin (IN1..IN4). The four switches are modeled as a single control line whose
  state is a 4-bit switch bitmask: bit i selects SW(i+1), 1 = closed.
  mux_control_disconnect() opens all four switches (drives every line low).

  Example:

    adgm3121 {
        compatible = "adi,adgm3121-gpio";
        in1-gpios = <&gpio0 0 GPIO_ACTIVE_HIGH>;
        in2-gpios = <&gpio0 1 GPIO_ACTIVE_HIGH>;
        in3-gpios = <&gpio0 2 GPIO_ACTIVE_HIGH>;
        in4-gpios = <&gpio0 3 GPIO_ACTIVE_HIGH>;
        #mux-control-cells = <0>;
        #mux-state-cells = <1>;
    };

compatible: "adi,adgm3121-gpio"

include: [base.yaml, mux-controller.yaml]

properties:
  in1-gpios:
    type: phandle-array
    required: true
    description: GPIO controlling switch SW1 (RF1A-RFCA).

  in2-gpios:
    type: phandle-array
    required: true
    description: GPIO controlling switch SW2 (RF2A-RFCA).

  in3-gpios:
    type: phandle-array
    required: true
    description: GPIO controlling switch SW3 (RF2B-RFCB).

  in4-gpios:
    type: phandle-array
    required: true
    description: GPIO controlling switch SW4 (RF1B-RFCB).

  "#mux-control-cells":
    const: 0

  "#mux-state-cells":
    const: 1

mux-state-cells:
  - switches
