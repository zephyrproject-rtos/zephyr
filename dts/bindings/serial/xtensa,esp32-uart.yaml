title: ESP32 Uart

description: >
    This binding gives a base representation of the ESP32 UART

inherits:
    !include uart.yaml

properties:
    compatible:
      constraint: "xtensa,esp32-uart"

    reg:
      required: true

    interrupts:
      required: true

    clocks:
      required: true
