description: Digilent 16550 UART

compatible: "digi_16550"

include: uart-controller.yaml

properties:
    reg:
      required: true

    reg-shift:
      type: int
      required: false
      description: quantity to shift the register offsets by

