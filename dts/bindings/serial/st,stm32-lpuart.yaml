description: STM32 LPUART

compatible: "st,stm32-lpuart"

include: uart-controller.yaml

properties:
    reg:
      required: true

    interrupts:
      required: true

    clocks:
      required: true
