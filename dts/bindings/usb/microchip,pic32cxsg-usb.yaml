description: |
    Microchip PIC32CXSG USB in device mode

compatible: "microchip,pic32cxsg-usb"

include:
  - name: usb-ep.yaml
  - name: pinctrl-device.yaml

properties:
  reg:
    required: true

  interrupts:
    required: true
