# Copyright (c) 2022 Nuvoton Technology Corporation
# SPDX-License-Identifier: Apache-2.0

description: Nuvoton NuMaker high-speed USB 2.0 device controller

compatible: "nuvoton,numaker-hsusbd"

include: [usb-ep.yaml, reset-device.yaml, pinctrl-device.yaml]

properties:
  reg:
    required: true

  interrupts:
    required: true

  resets:
    required: true

  clocks:
    required: true

  dma-buffer-size:
    type: int
    required: true
    description: |
      Size of DMA buffer in bytes

  allow-disable-usb-on-unplug:
    type: boolean
    description: |
      Some soc series allow USB function to be disabled temporarily on USB unplug. It can enable
      back on USB re-plug.
