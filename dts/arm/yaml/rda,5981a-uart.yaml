---
title: RDA 5981A UART
id: rda,5981a-uart
version: 0.1

description: >
    This binding gives a base representation of the rda5981a UART

inherits:
  - !include uart.yaml
  - !include zephyr_devices.yaml

properties:
  - compatible:
      type: string
      category: required
      description: compatible strings
      constraint: "rda,5981a-uart"

  - reg:
      type: array
      description: mmio register space
      generation: define
      category: required

  - interrupts:
      type: array
      category: required
      description: required interrupts
      generation: define
...
