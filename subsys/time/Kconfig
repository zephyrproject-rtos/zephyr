# Kconfig - time subsystem configurations

# Copyright (c) 2017 Linaro Limited
#
# SPDX-License-Identifier: Apache-2.0
#

menu Time

config EPOCH_TIME
	bool "Epoch time"
	default n
	help
	Enable epoch time api that allows you to manage current time based on
	hardware clock and rtc driver api (if available)

config EPOCH_TIME_INIT_PRIORITY
	int
	prompt "Startup priority of epoch time"
	depends on EPOCH_TIME
	default 60
	help
	The init priority of epoch time which should be larger than
	CONFIG_KERNEL_INIT_PRIORITY_DEVICE since epoch time depends on
	RTC driver api.

config EPOCH_TIME_RTC_WRITE_THROUGH
	bool "Update RTC if new epoch time is assigned"
	depends on EPOCH_TIME && RTC
	default y
	help
	If this option is set, RTC will be updated when epoch_time_set() is
	called.

endmenu
