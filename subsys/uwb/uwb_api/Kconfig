# Copyright 2026 NXP
# SPDX-License-Identifier: Apache-2.0

if UWB

config UWB_INITIALIZATION_POST_KERNEL
	bool "Post kernel initialization of UWB"
	default y
	help
	  Configure if initialization of UWB should be done during post kernel boot

config UWB_NOTIFICATION_CALLBACK_STACK_SIZE
	int "Size of default notification callback handler"
	default 10000
	help
	  Size of default notification callback handler

config UWB_NOTIFICATION_HANDLER_THREAD_PRIO
	int "Priority of default notification callback handler"
	default 5
	help
	  Priority of default notification callback handler

config UWB_NUM_SUPPORTED_ALIRO_PROTOCOL_VERSIONS
	int "Maximum number of Aliro supported protocol versions accepted from UWB device"
	default 2
	help
	  Maximum number of Aliro supported protocol versions accepted from UWB device

config UWB_NUM_SUPPORTED_CCC_PROTOCOL_VERSIONS
	int "Maximum number of CCC supported protocol versions accepted from UWB device"
	default 2
	help
	  Maximum number of CCC supported protocol versions accepted from UWB device

config UWB_NUM_SUPPORTED_CCC_UWB_CONFIG_ID
	int "Maximum number of CCC UWB Config IDs accepted from UWB device"
	default 2
	help
	  Maximum number of CCC UWB Config IDs accepted from UWB device

config UWB_NUM_SUPPORTED_CCC_PULSESHAPE_COMBO
	int "Maximum number of CCC pulseshape combo values accepted from UWB device"
	default 9
	help
	  Maximum number of CCC pulseshape combo values accepted from UWB device

endif
