# The kobject hash table is significantly larger when
# compiled with Zephyr SDK 0.16.1. So we need to enlarge
# the kobject rodata data area to accommodate that.
# Note this is just a workaround until we figure out
# what is going on with the SDK.
CONFIG_KOBJECT_RODATA_AREA_EXTRA_BYTES=256
