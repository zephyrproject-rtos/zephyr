tests:
  net.tls_credentials:
    tags:
      - net
      - tls
    depends_on: netif
  net.tls_credentials.trusted_tfm:
    filter: CONFIG_BUILD_WITH_TFM
    extra_args: OVERLAY_CONFIG=./prj-trusted-tfm.conf
    tags:
      - net
      - tls
      - trusted-firmware-m
    depends_on: netif
  net.tls_credentials.trusted_tfm_csr:
    filter: CONFIG_BUILD_WITH_TFM
    extra_args: OVERLAY_CONFIG=./prj-trusted-tfm-csr.conf
    tags:
      - net
      - tls
      - trusted-firmware-m
    depends_on: netif
