common:
  min_ram: 16
  depends_on: netif
  # eventfd API does not work with native_posix so exclude it here
  platform_exclude:
    - native_sim
    - native_sim/native/64
tests:
  net.config:
    tags:
      - net
      - iface
