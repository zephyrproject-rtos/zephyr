# SPDX-FileCopyrightText: Copyright The Zephyr Project Contributors
# SPDX-FileCopyrightText: Copyright (c) 2026 Dev It Wise
# SPDX-License-Identifier: Apache-2.0

CONFIG_ARCH_POSIX_LIBFUZZER=y

CONFIG_ASAN=y
CONFIG_ASAN_RECOVER=n
CONFIG_UBSAN=y

CONFIG_NETWORKING=y
CONFIG_NET_L2_DUMMY=y
CONFIG_NET_UDP=y
CONFIG_NET_IPV4=y
CONFIG_NET_IPV6=n
CONFIG_NET_DHCPV4_SERVER=y

# With ICMP probing on, the server does not answer a Discover directly:
# it sends an echo request and defers the Offer to a timer whose
# default, 1000 ms, is the whole window a case gets. The timer never
# expires inside the case that armed it, so dhcpv4_send_offer() is not
# reached at all. Turning the probe off keeps that path fuzzed.
CONFIG_NET_DHCPV4_SERVER_ICMP_PROBE_TIMEOUT=0

CONFIG_ENTROPY_GENERATOR=y
CONFIG_TEST_RANDOM_GENERATOR=y
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=8192
CONFIG_NET_SOCKETS_SERVICE_STACK_SIZE=8192

# net_recv_data() hands the packet to a receive thread unless there are
# no receive queues at all. A case parsed after the harness has moved on
# would be attributed to whichever input libFuzzer executed next, and a
# crash found that way would not replay, so the packet is processed in
# the caller's context instead.
CONFIG_NET_TC_RX_COUNT=0

CONFIG_NET_MGMT=y
CONFIG_NET_MGMT_EVENT=y

# A case is bounded by a fixed window of simulated time, so nothing in
# it may be deferred to a logging thread and land in the next case.
CONFIG_LOG=n

CONFIG_MAIN_STACK_SIZE=3072
