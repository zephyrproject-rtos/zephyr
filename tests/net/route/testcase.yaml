common:
  depends_on: netif
tests:
  net.route:
    min_ram: 16
    tags:
      - net
      - route
  net.route.unknown_ll:
    min_ram: 16
    tags:
      - net
      - route
    extra_configs:
      - CONFIG_NET_L2_ETHERNET=y
      - CONFIG_ETH_DRIVER=n
      - CONFIG_ASSERT=y
      - CONFIG_NET_IF_MAX_IPV6_COUNT=3
