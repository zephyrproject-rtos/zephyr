common:
  depends_on: netif
  tags:
    - net
    - socket
    - socket.register
tests:
  net.socket.register:
    extra_configs:
      - CONFIG_NET_SOCKETS_SOCKOPT_TLS=n
  net.socket.register.tls:
    extra_args:
      - EXTRA_CONF_FILE=overlay-tls.conf
