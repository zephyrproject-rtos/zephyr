common:
  depends_on: netif
  min_ram: 32
  tags: net socket userspace
tests:
  net.socket.tcp:
    extra_configs:
      - CONFIG_POSIX_API=n
  net.socket.tcp.posix:
    extra_configs:
      - CONFIG_POSIX_API=y
