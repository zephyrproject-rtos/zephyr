common:
  tags: net socket
  filter: TOOLCHAIN_HAS_NEWLIB == 1
  platform_exclude: intel_adsp_cavs15
  depends_on: netif
tests:
  net.socket.socketpair:
    min_ram: 21
  net.socket.socketpair.user:
    tags: userspace
    min_ram: 21
    extra_args: TEST_IN_USERSPACE=1
