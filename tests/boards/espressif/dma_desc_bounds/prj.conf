CONFIG_DMA=y
CONFIG_ZTEST=y
CONFIG_LOG=y
CONFIG_DMA_LOG_LEVEL_INF=y

# One descriptor puts the exhaustion boundary at a single descriptor's payload,
# so the test needs two small buffers rather than two of 64 KiB.
CONFIG_DMA_ESP32_MAX_DESCRIPTOR_NUM=1
