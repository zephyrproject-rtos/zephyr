The purpose of this test suite is to validate
NRFS services request handling performance.

These requests are propagated over IPC.

MRAM latency service and Temperature service
are used as benchmarks.
