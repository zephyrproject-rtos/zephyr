tests:
  config_dir.user_defined:
    platform_allow: native_posix
    build_only: true
    extra_args: APPLICATION_CONFIG_DIR:PATH=foo
  config_dir.board:
    platform_allow: native_posix
    build_only: true
