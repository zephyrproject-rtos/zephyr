/ {
	/delete-node/ deleted-by-foo;
	/delete-node/ added-by-bar;

	/* This node will be deleted by the `bar` snippet if applied. */
	added-by-foo {};

#ifdef CMAKE_DTS_CONFIGURE
	cmake-dts-configure {};
#endif /* CMAKE_DTS_CONFIGURE */
};
