common:
  depends_on:
    - scsi
    - ufshc
  tags:
    - scsi
    - ufs
  filter: dt_alias_exists("ufs0")
tests:
  scsi.ufs.scsi_basic:
    build_only: true
  scsi.ufs.scsi_rw:
    build_only: true
    extra_configs:
      - CONFIG_TEST_SCSI_UFS_RW=y
