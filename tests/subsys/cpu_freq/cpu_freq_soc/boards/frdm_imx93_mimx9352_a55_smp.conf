# Copyright 2026 NXP
# SPDX-License-Identifier: Apache-2.0

# A P-state reprograms the whole A55 cluster, so the benchmark pins a thread to
# each core in turn to measure every CPU. That needs the CPU affinity mask API.
CONFIG_SCHED_CPU_MASK=y
