# Clock control is used to apply DVFS oppoint to the
# NRF_IRON_HSFLL_LOCAL clock which is the CPU clock
CONFIG_CLOCK_CONTROL=y
# Ensure CPU clock is initialized to its lowest oppoint
# on boot.
CONFIG_CLOCK_CONTROL_NRF_IRON_HSFLL_LOCAL_REQ_LOW_FREQ=y
