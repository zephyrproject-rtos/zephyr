tests:
  shell.flash:
    min_flash: 64
    min_ram: 32
    filter: ( CONFIG_FLASH_SHELL )
    tags:
      - flash
      - shell
    platform_allow:
      - qemu_x86/atom
      - native_posix/native
      - native_sim/native
