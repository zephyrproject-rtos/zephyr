common:
  integration_platforms:
    - qemu_x86
    - qemu_x86_64
  platform_allow:
    # Representative platforms to make sure this builds without issues
    # and the analyzer actually runs and outputs something.
    - mps2/an385
    - qemu_cortex_a53
    - qemu_x86
    - qemu_x86_64
    - qemu_riscv32
    - qemu_riscv64
    - qemu_xtensa/dc233c/mmu
  tags:
    - debug
    - thread_analyzer
tests:
  debug.thread_analyzer.manual: {}
