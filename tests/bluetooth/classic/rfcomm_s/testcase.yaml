tests:
  bluetooth.classic.rfcomm.s:
    integration_platforms:
    - native_sim
    tags:
    - bluetooth
    - rfcomm
    harness: pytest
    harness_config:
      pytest_dut_scope: session
      fixture: usb_hci
    timeout: 600
