tests:
  kernel.pending:
    tags: kernel
  kernel.pending.minimallibc:
    filter: CONFIG_MINIMAL_LIBC_SUPPORTED
    tags:
      - kernel
      - libc
    extra_configs:
      - CONFIG_MINIMAL_LIBC=y
