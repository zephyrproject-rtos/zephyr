tests:
  kernel.condvar:
    ignore_faults: true
    tags:
      - kernel
      - userspace
      - condition_variables
  kernel.condvar.tracing:
    ignore_faults: true
    tags:
      - kernel
      - userspace
      - condition_variables
    platform_allow:
      - native_sim
    extra_configs:
      - CONFIG_TRACING=y
      - CONFIG_TRACING_USER=y
