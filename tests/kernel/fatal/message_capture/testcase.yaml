tests:
  kernel.logging.message_capture:
    harness: console
    harness_config:
      type: multi_line
      ordered: false
      regex:
        - "Current thread: (.*)"
        - "Caught system error -- reason (.*)"
