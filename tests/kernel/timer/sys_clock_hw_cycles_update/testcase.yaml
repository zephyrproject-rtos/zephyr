# Copyright 2026 NXP
# SPDX-License-Identifier: Apache-2.0

common:
  tags:
    - kernel
    - timer

tests:
  kernel.timer.sys_clock_hw_cycles_update:
    platform_allow:
      - qemu_cortex_m3
      - qemu_cortex_a53
      - qemu_x86_64
    integration_platforms:
      - qemu_cortex_m3
      - qemu_cortex_a53
      - qemu_x86_64
