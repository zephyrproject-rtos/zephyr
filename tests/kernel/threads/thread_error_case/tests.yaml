tests:
  kernel.threads.error.case:
    tags:
      - kernel
      - threads
      - userspace
    filter: CONFIG_ARCH_HAS_USERSPACE
    ignore_faults: true
    integration_platforms:
      - qemu_x86
