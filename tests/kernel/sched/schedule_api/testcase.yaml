tests:
  kernel.sched:
    min_ram: 40
    tags: kernel threads sched userspace
  kernel.sched.multiq:
    extra_args: CONF_FILE=prj_multiq.conf
    min_ram: 40
    tags: kernel threads sched userspace
