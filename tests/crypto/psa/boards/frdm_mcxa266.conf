# The SGI PKC PSA crypto driver builds its CPU/PKC workareas on the stack of the
# thread running the PSA operation, so both the main thread (psa_crypto_init())
# and the ztest thread need more room than the common values in prj.conf.
CONFIG_MAIN_STACK_SIZE=4096
CONFIG_ZTEST_STACK_SIZE=4096
