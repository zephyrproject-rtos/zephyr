tests:
-   test:
        filter: '( CONFIG_SRAM_SIZE >= 32 or CONFIG_DCCM_SIZE
            >= 32 or

            CONFIG_RAM_SIZE >= 32 )'
        tags: crypto mbedtls
        timeout: '200'
-   test_nios:
        arch_whitelist: nios2
        slow: true
        tags: crypto mbedtls
        timeout: '200'
