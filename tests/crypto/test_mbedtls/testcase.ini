[test]
tags = crypto mbedtls
kernel = unified
filter =  ( CONFIG_SRAM_SIZE >= 32 or CONFIG_DCCM_SIZE >= 32 or
	    CONFIG_RAM_SIZE >= 32 )

[test_nios]
tags = crypto mbedtls
kernel = unified
slow = true
arch_whitelist = nios2
