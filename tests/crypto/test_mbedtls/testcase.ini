[test]
tags = crypto mbedtls
kernel = nano
filter =  ( CONFIG_SRAM_SIZE > 32 or CONFIG_DCCM_SIZE > 32 or
	    CONFIG_RAM_SIZE > 32 )

[test_nios]
tags = crypto mbedtls
kernel = nano
slow = true
arch_whitelist = nios2
