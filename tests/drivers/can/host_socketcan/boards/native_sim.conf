# The companion on the host CAN bus runs at real (wall-clock) time, so slow the
# simulation down to match; otherwise the guest would exchange all its frames in
# simulated time before the companion has bound to the bus.
CONFIG_NATIVE_SIM_SLOWDOWN_TO_REAL_TIME=y
