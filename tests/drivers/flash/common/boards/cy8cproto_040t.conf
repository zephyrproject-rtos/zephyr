CONFIG_FLASH_FILL_BUFFER_SIZE=256

# 8KB SRAM is too small for the common test's default 2048/4096B stacks
CONFIG_MAIN_STACK_SIZE=1024
CONFIG_ZTEST_STACK_SIZE=1536

# Exercise test_get_size instead of skipping it (64KB flash)
CONFIG_TEST_DRIVER_FLASH_SIZE=65536
