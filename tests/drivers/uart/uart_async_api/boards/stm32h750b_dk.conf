CONFIG_DCACHE=y
CONFIG_NOCACHE_MEMORY=y
# Not enough MPU regions when both CONFIG_NOCACHE_MEMORY & CONFIG_USERSPACE are set
CONFIG_USERSPACE=n

# LPUART1 only works with BDMA, & the later can only access SRAM4
CONFIG_CODE_DATA_RELOCATION=y
CONFIG_UART_ASYNC_RELOCATE_SECTION="SRAM4"
