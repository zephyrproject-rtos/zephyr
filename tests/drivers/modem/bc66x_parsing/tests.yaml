# SPDX-License-Identifier: Apache-2.0

tests:
  modem.bc66x.parsing:
    tags: modem
    # Run this on native_sim since it's just string parsing and requires no hardware
    integration_platforms:
      - native_sim
