# SPDX-FileCopyrightText: Copyright The Zephyr Project Contributors
# SPDX-License-Identifier: Apache-2.0

CONFIG_ZTEST=y
# The test includes virtio_common.c directly, so keep the normal VirtIO
# library out of the build to avoid duplicate symbols.
CONFIG_VIRTIO=n
