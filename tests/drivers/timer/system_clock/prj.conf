CONFIG_ZTEST=y

# The interesting driver states are the ones reached with an empty kernel
# timeout queue, so keep the kernel from arming timeouts behind the test's
# back: a cooperative test thread is never time-sliced.
CONFIG_TIMESLICE_SIZE=0
CONFIG_ZTEST_THREAD_PRIORITY=-1
