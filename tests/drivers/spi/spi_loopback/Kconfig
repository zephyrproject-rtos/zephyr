mainmenu "SPI Loopback Test"

config ZEPHYR_BASE
	string
	option env="ZEPHYR_BASE"

config APPLICATION_BASE
        string
        option env="APPLICATION_BASE"

source "$APPLICATION_BASE/src/Kconfig"

source "$ZEPHYR_BASE/Kconfig.zephyr"
