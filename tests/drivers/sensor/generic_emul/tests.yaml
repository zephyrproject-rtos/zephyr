sample:
  description: Generic emulated sensor
  name: Generic emulated sensor

common:
  sysbuild: true
  platform_allow:
    - native_sim
  integration_platforms:
    - native_sim
tests:
  sensor.generic_emul: {}
  sensor.generic_emul.pm:
    extra_configs:
      - CONFIG_PM_DEVICE=y
      - CONFIG_PM_DEVICE_RUNTIME=y
