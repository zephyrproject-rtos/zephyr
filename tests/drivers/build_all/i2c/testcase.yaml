common:
  build_only: true
  tags:
    - drivers
    - i2c
tests:
  drivers.i2c.build:
    platform_allow:
      # will cover drivers without in-tree boards
      - qemu_cortex_m3
      - rpi_pico
    extra_args: "CONFIG_I2C=y"
