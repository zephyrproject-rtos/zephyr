tests:
  drivers.led_strip.build:
    harness: build
    min_ram: 32
    tags:
      - drivers
      - led_strip
    depends_on:
      - gpio
      - spi
  drivers.led_strip.build.rpi_pico_pio:
    harness: build
    platform_allow: rpi_pico
