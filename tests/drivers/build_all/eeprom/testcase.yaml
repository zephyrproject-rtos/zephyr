common:
  build_only: true
  tags:
    - drivers
    - eeprom
tests:
  drivers.eeprom.build:
    min_ram: 32
    platform_exclude: serpente/samd21e18a
    depends_on:
      - gpio
      - i2c
      - spi
