common:
  build_only: true
  tags:
    - drivers
    - ethernet
  depends_on:
    - eth
tests:
  net.ethernet.build.default: {}

  net.ethernet.build.spi:
    extra_args: DTC_OVERLAY_FILE="spi_devices.overlay"
    extra_configs:
      - CONFIG_SPI=y
    platform_allow:
      - native_sim
      - native_sim_64
