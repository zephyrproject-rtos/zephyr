common:
  build_only: true
  tags:
    - drivers
    - spi
tests:
  drivers.spi.build.sc18is606:
    # will cover drivers i2c based drivers
    platform_allow: native_sim
    tags: spi_nxp_sc18is606
    extra_args: "CONFIG_GPIO=y"

  drivers.spi.build.spi_cdns:
    # will cover drivers without in-tree boards
    platform_allow: qemu_cortex_m3
    tags: spi_cdns
    extra_args: "CONFIG_SPI=y"
