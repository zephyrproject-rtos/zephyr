tests:
  peripheral.eeprom:
    platform_allow: native_posix native_posix_64 qemu_x86
    tags: drivers userspace
  peripheral.eeprom.generic:
    build_only: true
    depends_on: eeprom
    tags: drivers userspace
