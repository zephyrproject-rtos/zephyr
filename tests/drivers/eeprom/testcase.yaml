tests:
  peripheral.eeprom:
    tags: drivers
    pre-filter: subsys_dt_compat_enabled_and_match("mtd", "eeprom")
    integration:
      - native_posix
  peripheral.eeprom.generic:
    build_only: true
    pre-filter: subsys_dt_compat_enabled_and_match("mtd", "eeprom")
    tags: drivers
    integration:
      - native_posix
