tests:
  drivers.eeprom:
    platform_whitelist: native_posix native_posix_64
    tags: drivers userspace
  peripheral.eeprom.generic:
    build_on_all: true
    depends_on: eeprom
    tags: drivers userspace
