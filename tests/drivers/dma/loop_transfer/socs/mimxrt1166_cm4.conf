# On CM4, SRAM1 (0x20000000) is the default zephyr,sram (DTCM).
# Relocating test buffers into SRAM1 overlaps the mcux-edma TCD
# pool. Use RAM (default heap) instead to avoid corruption.
CONFIG_CODE_DATA_RELOCATION=y
CONFIG_MEM_ATTR_HEAP=y
CONFIG_DMA_LOOP_TRANSFER_RELOCATE_SECTION="RAM"
