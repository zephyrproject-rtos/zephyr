tests:
  test_dma:
    min_ram: 16
    depends_on: dma
    tags: drivers dma
  test_dma_shell:
    depends_on: dma
    extra_args: CONF_FILE=prj_shell.conf
    min_ram: 16
    tags: drivers dma
    harness: keyboard
