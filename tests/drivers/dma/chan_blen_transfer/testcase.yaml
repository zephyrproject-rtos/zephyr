tests:
  drivers.dma:
    min_ram: 16
    pre-filter: subsys_dt_compat_enabled("dma")
    tags: drivers dma
  drivers.dma.interactive:
    pre-filter: subsys_dt_compat_enabled("dma")
    extra_args: CONF_FILE=prj_shell.conf
    min_ram: 16
    tags: drivers dma
    harness: keyboard
