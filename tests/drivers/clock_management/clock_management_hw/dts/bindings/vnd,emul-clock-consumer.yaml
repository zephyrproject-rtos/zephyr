# Copyright 2024 NXP
#
# SPDX-License-Identifier: Apache-2.0

description: |
  Binding for emulated clock consumer device. This device is used in testing
  to verify that clock states are applied as expected.

compatible: "vnd,emul-clock-consumer"

include: [clock-device.yaml]

properties:
  default-freq:
    type: int
    required: true
    description: |
      Frequency this consumer expects to read when applying default clock
      management state

  sleep-freq:
    type: int
    required: true
    description: |
      Frequency this consumer expects to read when applying sleep clock
      management state

  test1-freq:
    type: int
    required: true
    description: |
      Frequency this consumer expects to read when applying test1 clock
      management state

  test2-freq:
    type: int
    required: true
    description: |
      Frequency this consumer expects to read when applying test2 clock
      management state

  test3-freq:
    type: int
    required: true
    description: |
      Frequency this consumer expects to read when applying test3 clock
      management state

  freq-req-1:
    type: int
    required: true
    description: |
     First frequency this consumer will request from the clock subsystem

  freq-req-2:
    type: int
    required: true
    description: |
     Second frequency this consumer will request from the clock subsystem

  freq-req-3:
    type: int
    required: true
    description: |
     Third frequency this consumer will request from the clock subsystem
