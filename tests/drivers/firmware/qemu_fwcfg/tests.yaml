common:
  tags:
    - drivers
    - firmware

tests:
  drivers.firmware.qemu_fwcfg:
    harness: ztest
    timeout: 60
    integration_platforms:
      - qemu_x86
    platform_allow:
      - qemu_cortex_a53
      - qemu_x86
      - qemu_riscv64
