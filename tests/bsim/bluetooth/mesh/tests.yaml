common:
  timeout: 1800
  tags:
    - bluetooth
  depends_on: bsim
  harness: bsim
  platform_allow:
    - nrf52_bsim

tests:
  ###########################
  # Images reused by tests: #
  ###########################
  bluetooth.mesh.image:
    build_only: true
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_mesh_prj_conf

  bluetooth.mesh.image.pst:
    build_only: true
    extra_overlay_confs:
      - overlay_pst.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_mesh_prj_conf_overlay_pst_conf

  bluetooth.mesh.image.gatt:
    build_only: true
    extra_overlay_confs:
      - overlay_gatt.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_mesh_prj_conf_overlay_gatt_conf

  bluetooth.mesh.image.gatt_separate:
    build_only: true
    extra_overlay_confs:
      - overlay_gatt_separate.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_mesh_prj_conf_overlay_gatt_separate_conf

  bluetooth.mesh.image.low_lat:
    build_only: true
    extra_overlay_confs:
      - overlay_low_lat.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_mesh_prj_conf_overlay_low_lat_conf

  bluetooth.mesh.image.workq_sys:
    build_only: true
    extra_overlay_confs:
      - overlay_workq_sys.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_mesh_prj_conf_overlay_workq_sys_conf

  bluetooth.mesh.image.multi_adv_sets:
    build_only: true
    extra_overlay_confs:
      - overlay_multi_adv_sets.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_mesh_prj_conf_overlay_multi_adv_sets_conf

  bluetooth.mesh.image.lpn_scan_on:
    build_only: true
    extra_overlay_confs:
      - overlay_lpn_scan_on.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_mesh_prj_conf_overlay_lpn_scan_on_conf

  bluetooth.mesh.image.gatt_workq_sys:
    build_only: true
    extra_overlay_confs:
      - overlay_gatt.conf
      - overlay_workq_sys.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_mesh_prj_conf_overlay_gatt_conf_overlay_workq_sys_conf

  bluetooth.mesh.image.gatt_low_lat:
    build_only: true
    extra_overlay_confs:
      - overlay_gatt.conf
      - overlay_low_lat.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_mesh_prj_conf_overlay_gatt_conf_overlay_low_lat_conf

  bluetooth.mesh.image.pst_gatt:
    build_only: true
    extra_overlay_confs:
      - overlay_pst.conf
      - overlay_gatt.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_mesh_prj_conf_overlay_pst_conf_overlay_gatt_conf

  bluetooth.mesh.image.gatt_multi_adv_sets:
    build_only: true
    extra_overlay_confs:
      - overlay_gatt.conf
      - overlay_multi_adv_sets.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: >-
        tests_bsim_bluetooth_mesh_prj_conf_overlay_gatt_conf_overlay_multi_adv_sets_conf

  bluetooth.mesh.image.pst_gatt_workq_sys:
    build_only: true
    extra_overlay_confs:
      - overlay_pst.conf
      - overlay_gatt.conf
      - overlay_workq_sys.conf
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: >-
        tests_bsim_bluetooth_mesh_prj_conf_overlay_pst_conf_overlay_gatt_conf_overlay_workq_sys_conf

  #################
  # Actual tests: #
  #################

  bluetooth.mesh.access:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/access
    required_applications:
      - application: bluetooth.mesh.image

  bluetooth.mesh.advertiser:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/advertiser
    required_applications:
      - application: bluetooth.mesh.image
      - application: bluetooth.mesh.image.gatt
      - application: bluetooth.mesh.image.gatt_multi_adv_sets
      - application: bluetooth.mesh.image.gatt_separate
      - application: bluetooth.mesh.image.gatt_workq_sys
      - application: bluetooth.mesh.image.low_lat
      - application: bluetooth.mesh.image.multi_adv_sets
      - application: bluetooth.mesh.image.workq_sys

  bluetooth.mesh.beacon:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/beacon
    required_applications:
      - application: bluetooth.mesh.image

  bluetooth.mesh.blob_mdls:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/blob_mdls
    required_applications:
      - application: bluetooth.mesh.image
      - application: bluetooth.mesh.image.pst

  bluetooth.mesh.bridge:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/bridge
    required_applications:
      - application: bluetooth.mesh.image
      - application: bluetooth.mesh.image.pst

  bluetooth.mesh.cdb:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/cdb
    required_applications:
      - application: bluetooth.mesh.image

  bluetooth.mesh.comp_data:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/comp_data
    required_applications:
      - application: bluetooth.mesh.image

  bluetooth.mesh.dfu:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/dfu
    required_applications:
      - application: bluetooth.mesh.image
      - application: bluetooth.mesh.image.pst

  bluetooth.mesh.friendship:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/friendship
    required_applications:
      - application: bluetooth.mesh.image
      - application: bluetooth.mesh.image.low_lat
      - application: bluetooth.mesh.image.lpn_scan_on

  bluetooth.mesh.heartbeat:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/heartbeat
    required_applications:
      - application: bluetooth.mesh.image

  bluetooth.mesh.iv_index:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/iv_index
    required_applications:
      - application: bluetooth.mesh.image

  bluetooth.mesh.large_comp_data:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/large_comp_data
    required_applications:
      - application: bluetooth.mesh.image.pst

  bluetooth.mesh.op_agg:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/op_agg
    required_applications:
      - application: bluetooth.mesh.image

  bluetooth.mesh.persistence:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/persistence
    required_applications:
      - application: bluetooth.mesh.image.pst

  bluetooth.mesh.priv_beacon:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/priv_beacon
    required_applications:
      - application: bluetooth.mesh.image
      - application: bluetooth.mesh.image.gatt

  bluetooth.mesh.provision:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/provision
    required_applications:
      - application: bluetooth.mesh.image
      - application: bluetooth.mesh.image.gatt
      - application: bluetooth.mesh.image.pst

  bluetooth.mesh.proxy_sol:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/proxy_sol
    required_applications:
      - application: bluetooth.mesh.image.pst_gatt
      - application: bluetooth.mesh.image.pst_gatt_workq_sys

  bluetooth.mesh.replay_cache:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/replay_cache
    required_applications:
      - application: bluetooth.mesh.image.pst

  bluetooth.mesh.sar:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/sar
    required_applications:
      - application: bluetooth.mesh.image
      - application: bluetooth.mesh.image.pst

  bluetooth.mesh.scanner:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/scanner
    required_applications:
      - application: bluetooth.mesh.image

  bluetooth.mesh.suspend:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/suspend
    required_applications:
      - application: bluetooth.mesh.image
      - application: bluetooth.mesh.image.gatt
      - application: bluetooth.mesh.image.gatt_low_lat
      - application: bluetooth.mesh.image.low_lat

  bluetooth.mesh.transport:
    build: false
    harness_config:
      fixture: bsim_multi_test
      tests_scripts:
        - tests_scripts/transport
    required_applications:
      - application: bluetooth.mesh.image
      - application: bluetooth.mesh.image.low_lat
