tests:
  bluetooth.host.scan.start_stop:
    tags:
      - bluetooth
    platform_allow:
      - nrf52_bsim/native
    timeout: 2
    harness: bsim
    harness_config:
      bsim_exe_name:
        - tests_bsim_bluetooth_host_scan_start_stop_prj_conf
      bsim_test_ids:
        - scanner
        - periodic_adv
      bsim_verbosity: 2
      bsim_sim_length: 5e6
