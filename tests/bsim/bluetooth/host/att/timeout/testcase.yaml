tests:
  bluetooth.host.att.timeout:
    tags:
      - bluetooth
    platform_allow:
      - nrf52_bsim/native
    timeout: 20
    harness: bsim
    harness_config:
      bsim_exe_name:
        - tests_bsim_bluetooth_host_att_timeout_prj_conf
      bsim_verbosity: 2
      bsim_sim_length: 200e6
      bsim_test_ids:
        - the_test
        - the_test
      bsim_options:
        - -D=2
        - -RealEncryption=1
