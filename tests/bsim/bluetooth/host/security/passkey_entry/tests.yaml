common:
  tags:
    - bluetooth
  depends_on: bsim
  platform_allow:
    - nrf52_bsim/native
  harness: bsim

tests:
  bluetooth.host.security.passkey_entry:
    harness_config:
      fixture: bsim_multi_test
      bsim_exe_name: tests_bsim_bluetooth_host_security_passkey_entry_prj_conf
      tests_scripts:
        - tests_scripts/passkey_entry.sh
