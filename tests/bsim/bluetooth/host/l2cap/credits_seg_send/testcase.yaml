common:
  build_only: true
  tags:
    - bluetooth
  platform_allow:
    - nrf52_bsim/native
  harness: bsim

tests:
  bluetooth.host.l2cap.credits_seg_send:
    harness_config:
      bsim_exe_name: tests_bsim_bluetooth_host_l2cap_credits_seg_send_prj_conf
  bluetooth.host.l2cap.credits_seg_send_mps_equal_acl_mtu:
    harness_config:
      bsim_exe_name: tests_bsim_bluetooth_host_l2cap_credits_seg_send_overlay-mps_equal_acl_mtu_conf
    extra_args: EXTRA_CONF_FILE="overlay-mps-equal-acl-mtu.conf"
