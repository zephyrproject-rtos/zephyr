tests:
  arch.x86.syscalls.cpu_scrubs_regs:
    arch_allow: x86
    filter: CONFIG_ARCH_HAS_USERSPACE
    tags:
      - kernel
      - security
      - userspace
      - scrub
      - registers
    integration_platforms:
      - qemu_x86
