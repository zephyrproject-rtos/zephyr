tests:
  arch.arm64.mmu:
    platform_allow: qemu_cortex_a53
  arch.arm64.mmu.userspace:
    platform_allow: qemu_cortex_a53
    extra_configs:
      - CONFIG_USERSPACE=y
      - CONFIG_MAX_XLAT_TABLES=24
