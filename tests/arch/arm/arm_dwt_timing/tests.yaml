common:
  filter: CONFIG_CORTEX_M_DWT and not CONFIG_CORTEX_M_SYSTICK
          and not CONFIG_CMSIS_CORE_HAS_SYSTEM_CORE_CLOCK
  tags:
    - arm
    - timing
  arch_allow: arm
  platform_type:
    - mcu
  integration_platforms:
    - frdm_rw612
tests:
  arch.arm.dwt_timing:
    timeout: 10
