tests:
  arch.common.ramfunc:
    filter: CONFIG_ARCH_HAS_RAMFUNC_SUPPORT
    tags:
      - arm
      - userspace
    arch_allow:
      - arm
      - riscv
    extra_configs:
      - CONFIG_COMMON_LIBC_MALLOC_ARENA_SIZE=0
