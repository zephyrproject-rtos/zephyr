tests:
  arch.riscv.stack_unwind:
    arch_allow: riscv
    harness: console
    harness_config:
      type: multi_line
      regex:
        - ".* Call trace begin:"
        - ".* 0: fp: \\w+   ra: \\w+"
        - ".* 1: fp: \\w+   ra: \\w+"
