tests:
  benchmark.application:
    arch_whitelist: x86 arm
    min_ram: 32
    tags: benchmark
    slow: true
    timeout: 300
  test_posix:
    arch_whitelist: posix
    min_ram: 32
    tags: benchmark
