tests:
  test:
    arch_whitelist: x86 arm
    min_ram: 32
    tags: benchmark
    slow: true
    timeout: 300
  test_posix:
    arch_whitelist: posix
    tags: benchmark
  test_debug:
    arch_whitelist: posix
    tags: benchmark
    extra_configs:
      - CONFIG_KERNEL_DEBUG=y
