common:
  harness: console
  harness_config:
    type: one_line
    regex:
      - "Benchmark completed"
  timeout: 10
  tags: crypto
tests:
  benchmark.crypto.mbedtls:
    filter: CONFIG_TEST_RANDOM_GENERATOR
    integration_platforms:
      - qemu_x86
