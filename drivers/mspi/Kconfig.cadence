# Copyright (c) 2025 Siemens Mobility GmbH
# SPDX-License-Identifier: Apache-2.0

config MSPI_CDNS
	bool "Cadence MSPI driver"
	default y
	depends on DT_HAS_CDNS_MSPI_CONTROLLER_ENABLED
	# GPIO is required for ce_gpios despite the field being ignored
	select GPIO
	select PINCTRL
	select MSPI_TIMING
	help
	  Enable driver for Cadence MSPI peripheral
