# Copyright (c) 2026 RAKwireless Technology Limited
# SPDX-License-Identifier: Apache-2.0

config ETH_WIZNET
	bool
	help
	  Shared MACRAW core for the WIZnet stand-alone Ethernet controllers.

if ETH_W5100S || ETH_W5500 || ETH_W6100 || ETH_W6300

config ETH_WIZNET_RX_THREAD_STACK_SIZE
	int "Stack size for internal incoming packet handler"
	default 1024
	help
	  Size of the stack used for internal thread which is ran for
	  incoming packet processing.

config ETH_WIZNET_RX_THREAD_PRIO
	int "Priority for internal incoming packet handler"
	default 2
	help
	  Priority level for internal thread which is ran for incoming
	  packet processing.

config ETH_WIZNET_TIMEOUT
	int "IP buffer timeout"
	default 100
	help
	  Given timeout in milliseconds. Maximum amount of time
	  that the driver will wait from the IP stack to get
	  a memory buffer before the Ethernet frame is dropped.

config ETH_WIZNET_MONITOR_PERIOD
	int "Monitor task execution period"
	default 500
	help
	  Monitor task execution period in milliseconds. The monitor task is
	  periodically executed to detect and report any changes in the
	  link status to the operating system.

config ETH_WIZNET_POLL_PERIOD
	int "Polling period in milliseconds"
	default 10
	help
	  Polling interval in milliseconds for RX data and link status
	  when operating in polling mode (no int-gpios defined in
	  devicetree).

endif # any WIZnet driver

# zephyr-keep-sorted-start
source "drivers/ethernet/wiznet/Kconfig.w5100s"
source "drivers/ethernet/wiznet/Kconfig.w5500"
source "drivers/ethernet/wiznet/Kconfig.w6100"
source "drivers/ethernet/wiznet/Kconfig.w6300"
# zephyr-keep-sorted-stop
