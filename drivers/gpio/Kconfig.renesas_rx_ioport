# Copyright (c) 2026 Renesas Electronics Corporation
# SPDX-License-Identifier: Apache-2.0

config GPIO_RX_IOPORT
	bool "Renesas RX GPIO IO port driver"
	default y
	depends on DT_HAS_RENESAS_RX_GPIO_IOPORT_ENABLED
	select PINCTRL
	help
	  Enable the Renesas RX GPIO IO port driver.

if GPIO_RX_IOPORT

config GPIO_RX_HAS_VBTICTLR
	bool "Support VBATT input control"
	help
	  Enable for Renesas RX which support VBATT input control.

endif # GPIO_RX_IOPORT
