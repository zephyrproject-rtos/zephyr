#ST Microelectronics STTS22H temperature sensor

#Copyright (c) 2024 Javad Rahimipetroudi <javad.rahimipetroudi@mind.be>
# SPDX-License-Identifier: Apache-2.0

menuconfig STTS22H
	bool "STTS22H temperature sensor"
	default y
	depends on DT_HAS_ST_STTS22H_ENABLED
	depends on ZEPHYR_HAL_ST_MODULE
	select I2C
	select HAS_STMEMSC
	select USE_STDC_STTS22H
	help
	  Enable driver for STTS22H I2C-based temperature sensor.

if STTS22H

choice STTS22H_TRIGGER_MODE
	prompt "Trigger mode"
	default STTS22H_TRIGGER_NONE
	help
	  Specify the type of triggering to be used by the driver.

config STTS22H_TRIGGER_NONE
	bool "No trigger"

config STTS22H_TRIGGER_GLOBAL_THREAD
	bool "Use global thread"
	depends on GPIO
	select STTS22H_TRIGGER

config STTS22H_TRIGGER_OWN_THREAD
	bool "Use own thread"
	depends on GPIO
	select STTS22H_TRIGGER

endchoice # STTS22H_TRIGGER_MODE

config STTS22H_TRIGGER
	bool

config STTS22H_THREAD_PRIORITY
	int "Thread priority"
	depends on STTS22H_TRIGGER_OWN_THREAD
	default 10
	help
	  Priority of thread used by the driver to handle interrupts.

config STTS22H_THREAD_STACK_SIZE
	int "Thread stack size"
	depends on STTS22H_TRIGGER_OWN_THREAD
	default 1024
	help
	  Stack size of thread used by the driver to handle interrupts.

menu "Attributes"

config STTS22H_TEMP_HI_THRESHOLD
	depends on STTS22H_TRIGGER
	int "High temperature threshold alarm"
	default 50
	help
	  HIGH temperature threshold to trigger an alarm

config STTS22H_TEMP_LO_THRESHOLD
	depends on STTS22H_TRIGGER
	int "Low temperature threshold alarm"
	default 10
	help
	  LOW temperature threshold to trigger an alarm

config STTS22H_SAMPLING_RATE
	int "Output data rate"
	range 0 4
	default 0
	help
	  Sensor output data rate in freerun mode:
	  0:  1 Hz
	  1:  25 Hz
	  2:  50 Hz
	  3:  100 Hz
	  4:  200 Hz

endmenu

endif # STTS22H
