# Copyright 2022 Google LLC
#
# SPDX-License-Identifier: Apache-2.0

menuconfig BATTERY
	bool "Battery drivers"
	help
	  Enable battery driver configuration.

if BATTERY

module = BATTERY
module-str = battery
source "subsys/logging/Kconfig.template.log_config"

source "drivers/battery/sbs_gauge/Kconfig"

endif # BATTERY
