# OpenOCD configuration for TI TM4C123GXL LaunchPad
# Uses the on-board TI ICDI (In-Circuit Debug Interface)

source [find interface/ti-icdi.cfg]
transport select hla_jtag
source [find target/stellaris.cfg]
