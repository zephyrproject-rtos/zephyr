# SPDX-FileCopyrightText: Copyright Alif Semiconductor
# SPDX-License-Identifier: Apache-2.0

CONFIG_ARM_AARCH32_MMU=y

CONFIG_XIP=n

CONFIG_THREAD_STACK_INFO=y

# Enable Timer and Sys clock
CONFIG_SYS_CLOCK_TICKS_PER_SEC=1000
CONFIG_ARM_ARCH_TIMER=y

# Enable UART driver
CONFIG_SERIAL=y

# Enable serial port
CONFIG_UART_INTERRUPT_DRIVEN=y

# Enable console
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# Enable Monitor mode initialization for GICv3 system register access.
CONFIG_AARCH32_ARMV8_A_MONITOR_INIT=y

# Enable SMP. The switch-based context path is required for SMP.
CONFIG_USE_SWITCH=y
CONFIG_SMP=y
CONFIG_MP_MAX_NUM_CPUS=4
CONFIG_CACHE_MANAGEMENT=y

# Secondary cores are held powered-off at reset on the FVP Base model and
# must be powered on via the FVP power controller (no PSCI/secure firmware).
CONFIG_PM_CPU_OPS=y
CONFIG_PM_CPU_OPS_FVP=y
CONFIG_PM_CPU_OPS_PSCI=n
