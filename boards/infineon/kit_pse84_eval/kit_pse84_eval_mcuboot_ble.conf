# BLE-only DFU flow companion configuration.
# Disables MCUboot serial recovery transport so the bootloader can safely
# route its console logs back to the primary KitProg3 UART (SCB2).
CONFIG_MCUBOOT_SERIAL=n
