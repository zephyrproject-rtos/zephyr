identifier: qemu_tc4x
name: QEMU emulation for AURIX TC4Dx (TC1.8)
type: qemu
simulation:
  - name: qemu
arch: tricore
toolchain:
  - cross-compile
ram: 240
supported:
  - gpio
  - uart
testing:
  default: true
  ignore_tags:
    - net
    - bluetooth
    - cache
vendor: infineon
