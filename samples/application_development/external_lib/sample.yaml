sample:
  name: External Library
tests:
  test:
    tags: external
    platform_exclude: qemu_x86_64
    harness: console
    harness_config:
      type: multi_line
      regex:
        - "Hello World!"
        - "mylib says: Hello World!"
