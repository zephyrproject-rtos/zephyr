sample:
  name: echo Sample
tests:
  sample.console.echo:
    integration_platforms:
      - mps2/an385
    filter: CONFIG_UART_CONSOLE and CONFIG_SERIAL_SUPPORT_INTERRUPT
    tags: console
    harness: keyboard
