sample:
  name: getchar sample
tests:
  sample.drivers.console.getchar:
    integration_platforms:
      - mps2/an385
    filter: CONFIG_UART_CONSOLE and CONFIG_SERIAL_SUPPORT_INTERRUPT
    tags: console
    harness: keyboard
