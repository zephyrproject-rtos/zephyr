sample:
  name: Fat filesystem sample
common:
  tags: filesystem
tests:
  sample.filesystem.fat_fs:
    pre-filter: subsys_dt_compat_enabled("mmc")
  sample.filesystem.fat_fs.overlay:
    platform_whitelist: nrf52840_blip
