# Copyright 2026 NXP
# SPDX-License-Identifier: Apache-2.0

# Re-initialize FlexSPI even in XIP mode so Zephyr configures PortB1 (NAND).
# Without this, memc skips init when XIP is active and PortB1 is never set up.
CONFIG_MEMC_MCUX_FLEXSPI_INIT_XIP=y

# Must be > MEMC_INIT_PRIORITY so FlexSPI controller is ready first.
CONFIG_FLASH_MCUX_FLEXSPI_NAND_INIT_PRIORITY=51

# FTL block device backend for littlefs
CONFIG_DISK_DRIVER_FTL=y
CONFIG_DISK_ACCESS=y
CONFIG_APP_LITTLEFS_STORAGE_BLK_FTL=y
CONFIG_FS_LITTLEFS_BLK_DEV=y
CONFIG_FS_LITTLEFS_CACHE_SIZE=2048
CONFIG_FS_LITTLEFS_LOOKAHEAD_SIZE=64

# LittleFS + Dhara + FlexSPI HAL call chain exceeds default stack size.
CONFIG_MAIN_STACK_SIZE=16384
