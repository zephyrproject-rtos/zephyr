sample:
  name: USB MTP device class sample
tests:
  sample.usb_device_next.mtp:
    depends_on: usbd
    tags: usb
    harness: console
    harness_config:
      type: one_line
      regex:
        - "USB device support enabled"
