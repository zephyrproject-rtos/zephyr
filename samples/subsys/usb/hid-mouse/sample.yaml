sample:
  name: USB HID mouse sample
tests:
  sample.usb.hid-mouse:
    pre-filter: subsys_dt_compat_enabled("usb") and subsys_dt_compat_enabled("gpio")
    harness: button
    filter: dt_alias_exists("sw0")
    tags: usb
