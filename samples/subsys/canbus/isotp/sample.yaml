sample:
  name: ISO-TP lib sample
tests:
  sample.subsys.canbus.isotp:
    tags: CAN
    pre-filter: dt_bus_enabled("can")
    harness: console
    harness_config:
      type: one_line
      regex:
        - "(.*)Got 248 bytes in total"
