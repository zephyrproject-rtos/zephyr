sample:
  name: FIDO2 Authenticator
common:
  depends_on:
    - usbd
  filter: dt_compat_enabled("zephyr,hid-device")
  harness: console
  harness_config:
    type: one_line
    regex:
      - "FIDO2 authenticator ready"
  integration_platforms:
    - weact_stm32wb55_core
    - nrf54lm20dk/nrf54lm20a/cpuapp
tests:
  sample.authentication.fido2: {}
