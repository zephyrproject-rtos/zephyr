sample:
  description: EDAC shell sample application
  name: edac shell
common:
    tags: edac
    harness: console
    harness_config:
      type: one_line
      regex:
        - "EDAC shell application initialized"

tests:
  sample.subsys.edac:
    platform_allow: ehl_crb
    tags: edac
