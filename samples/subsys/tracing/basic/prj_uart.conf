# Overlay: "test" format streamed over a dedicated UART.
# Applied on top of prj.conf with EXTRA_CONF_FILE=prj_uart.conf.
CONFIG_IDLE_STACK_SIZE=4096
CONFIG_TRACING_TEST=y
CONFIG_TRACING_BACKEND_UART=y
