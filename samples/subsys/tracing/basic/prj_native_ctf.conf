# Overlay: CTF written to a file by the native (POSIX) backend.
# Applied on top of prj.conf with EXTRA_CONF_FILE=prj_native_ctf.conf.
CONFIG_TRACING_SYNC=y
CONFIG_TRACING_BACKEND_POSIX=y
CONFIG_TRACING_PACKET_MAX_SIZE=64
