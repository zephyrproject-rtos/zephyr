# Overlay: Percepio Tracealyzer recorder (requires the percepio module).
# Applied on top of prj.conf with EXTRA_CONF_FILE=prj_percepio.conf. Selecting
# the Percepio recorder here overrides the CTF format from the baseline.
CONFIG_PERCEPIO_TRACERECORDER=y
