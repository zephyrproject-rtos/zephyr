common:
  tags: llext edk
  arch_allow:
    - arm
  filter: CONFIG_ARCH_HAS_USERSPACE
sample:
  description: EDK sample application
  name: EDK sample application
tests:
  sample.edk.app:
    build_only: true
    tags: edk llext
