sample:
  description: TBD
  name: TBD
tests:
  test:
    build_only: true
    tags: apps
  test_stack_guard:
    arch_whitelist: arm
    build_only: true
    extra_args: CONF_FILE=prj_stack_guard.conf
    filter: CONFIG_MPU_STACK_GUARD
    tags: apps
