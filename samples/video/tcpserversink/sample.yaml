sample:
  name: Video TCP server sink
tests:
  sample.video.tcpserversink:
    build_only: true
    tags: video net socket
    platform_whitelist: mimxrt1064_evk
    pre-filter: subsys_dt_compat_enabled("video") and subsys_dt_compat_enabled("ethernet")
