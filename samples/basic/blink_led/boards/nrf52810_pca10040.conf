# The nrfx Hardware PWM driver cannot be used in this sample since the driver
# does not allow to set a period longer than ~262 ms. Disabling this driver
# will make the nRF SW PWM driver enabled by default.
CONFIG_PWM_NRFX=n
