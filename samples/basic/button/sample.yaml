sample:
  name: Button Sample
tests:
  sample.basic.button:
    tags: button gpio
    filter: dt_compat_enabled_with_alias("gpio-keys", "sw0")
    pre-filter: subsys_dt_compat_enabled("gpio")
    harness: button
