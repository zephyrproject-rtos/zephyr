sample:
  name: BMM150 Sensor Sample
tests:
  sample.sensor.bmm150:
    harness: sensor
    tags: sensors
    pre-filter: subsys_dt_compat_enabled("gpio") and subsys_dt_compat_enabled("i2c")
    filter: dt_compat_enabled("bosch,bmm150")
