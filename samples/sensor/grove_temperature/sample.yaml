sample:
  name: Grove Temperature Sensor
tests:
  sample.sensor.grove_temperature:
    min_flash: 33
    tags: drivers sensor grove temperature
    harness: grove
    pre-filter: subsys_dt_compat_enabled("adc")
    filter: dt_compat_enabled("grove,temperature")
