sample:
  name: SX9500 Sensor Sample
tests:
  sample.sensor.sx9500:
    harness: sensor
    tags: sensors
    pre-filter: subsys_dt_compat_enabled("i2c") and subsys_dt_compat_enabled("gpio")
    filter: dt_compat_enabled("semtech,sx9500")
