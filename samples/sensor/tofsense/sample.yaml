sample:
  name: Sample for Nooploop TOFsens Time Of Flight sensor
tests:
  sample.sensor.tofsense:
    depends_on:
      - uart
      - tofsense
    tags:
      - sensors
    integration_platforms:
      - nucleo_u575zi_q
    harness: console
    harness_config:
      type: multi_line
      regex:
        - "distance is .*"
