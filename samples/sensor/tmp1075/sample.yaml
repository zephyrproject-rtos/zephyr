sample:
  name: TMP 1075 Temperature Sensor Sample
tests:
  sample.sensor.tmp1075:
    harness: console
    tags: sensors
    depends_on:
      - i2c
      - gpio
    filter: dt_compat_enabled("ti,tmp1075")
    harness_config:
      type: multi_line
      regex:
        - "Temperature: *.*C"
