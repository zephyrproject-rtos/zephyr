.. zephyr:code-sample: sensor-accel-streaming
   :name: Accelerometer sensor streaming
   :relevant-api: sensor_interface rtio

Overview
********

Sample application that streams multiple accelerotomers using read and decode
sensor APIs. Listens for data ready and tap events, automatically
fetching data with data ready events.
