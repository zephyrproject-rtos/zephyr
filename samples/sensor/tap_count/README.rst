.. zephyr:code-sample:: sensor-tap-count
   :name: Tap event counting
   :relevant-api: sensor_interface rtio

Overview
********

Sample application that assigns a single trigger handler to the tap
event on all accelerometer channels. Counts and prints out total tap
count.
