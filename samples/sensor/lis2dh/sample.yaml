sample:
  name: LIS2DH Accelerometer Monitor
tests:
  sample.sensor.lis2dh:
    harness: sensor
    tags: samples sensor
    pre-filter: subsys_dt_compat_enabled("i2c") and dt_compat_enabled("st,lis2dh")
