sample:
  name: DAC driver sample
tests:
  sample.drivers.dac:
    tags: DAC
    platform_whitelist: frdm_k64f nucleo_l073rz twr_ke18f
    pre-filter: subsys_dt_compat_enabled("dac")
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Generating sawtooth signal at DAC channel ([0-9]*)."
