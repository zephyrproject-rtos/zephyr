sample:
  name: dmic i2s sample
tests:
  sample.drivers.i2s.dmic_i2s:
    tags: i2s
    platform_allow:
      - apollo510_evb
    harness: console
    harness_config:
      type: multi_line
      regex:
        - "DMIC I2S sample"
        - "Streams stopped"
