sample:
  name: RTC Driver Sample
tests:
  sample.driver.rtc:
    tags: drivers
    harness: console
    harness_config:
        type: multi_line
        ordered: true
        regex:
            - "Test RTC driver"
            - "Alarm"
    depends_on: rtc
