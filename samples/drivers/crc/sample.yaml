sample:
  name: CRC Sample
tests:
  sample.drivers.crc:
    depends_on: crc
    tags:
      - drivers
      - crc
    integration_platforms:
      - ek_ra8m1
    harness: console
    harness_config:
      type: one_line
      regex:
        - "verification succeeded"
