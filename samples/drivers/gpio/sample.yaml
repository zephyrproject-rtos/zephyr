sample:
  name: GPIO toggling
tests:
  sample.driver.gpio:
    tags: drivers
    filter: DT_ALIAS_LED0_GPIOS_CONTROLLER and DT_ALIAS_SW0_GPIOS_CONTROLLER
    harness: console
    harness_config:
        type: one_line
        regex:
            - "Toggling (.*)"
    depends_on: gpio
