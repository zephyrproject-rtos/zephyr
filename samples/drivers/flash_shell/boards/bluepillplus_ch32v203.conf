CONFIG_USE_DT_CODE_PARTITION=y

# Shrink the defaults to fit within the 52 KiB of flash and 20 KiB of RAM.
CONFIG_FLASH_SHELL_TEST_COMMANDS=n
CONFIG_KERNEL_SHELL=n
CONFIG_DATE_SHELL=n
CONFIG_LOG_MODE_MINIMAL=y
