sample:
  name: KSCAN driver sample
tests:
  sample.drivers.kscan:
    tags: drivers
    pre-filter: subsys_dt_compat_enabled("kscan")
    harness: console
    harness_config:
        type: multi_line
        ordered: true
        regex:
            - "kb data(.*)"
