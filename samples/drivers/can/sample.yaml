sample:
  name: CAN driver sample
tests:
  sample.drivers.can:
    tags: CAN
    pre-filter: dt_bus_enabled("can")
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Counter received: (.*)"
