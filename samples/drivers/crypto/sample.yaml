sample:
  description: An example to illustrate the usage of
    crypto APIs
  name: Crypto APIs
common:
  tags: crypto
  harness: console
  min_ram: 16
tests:
  test-mbedtls:
    extra_args: CONF_FILE=prj_mtls_shim.conf
  test-micro:
    tags: micro
