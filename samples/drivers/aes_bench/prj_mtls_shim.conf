CONFIG_MBEDTLS=y
CONFIG_MBEDTLS_ENABLE_HEAP=y
CONFIG_MBEDTLS_HEAP_SIZE=16384

CONFIG_CRYPTO_MBEDTLS_SHIM=y

# prj.conf's 1024-byte CONFIG_MAIN_STACK_SIZE is enough for the HW CRYP
# driver but not for the mbedTLS/PSA software AES call chain.
CONFIG_MAIN_STACK_SIZE=8192
