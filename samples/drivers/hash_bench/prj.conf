CONFIG_CRYPTO=y
CONFIG_CPU_LOAD=y

# The benchmark loop calls hash_compute() thousands of times per case;
# keep the log level quiet so the BENCH result lines stay readable.
CONFIG_CRYPTO_LOG_LEVEL_WRN=y
