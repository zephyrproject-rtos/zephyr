# Reduce connection count to compensate for increased net_buf size
# (atomic_t ref field increases memory usage by ~3 bytes per buffer)
# 624-byte overflow with 62 connections; reducing to 50 saves ~10KB
CONFIG_BT_MAX_CONN=50
