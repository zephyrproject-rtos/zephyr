# Reduce main stack size to compensate for increased net_buf size
# (atomic_t ref field increases memory usage by ~3 bytes per buffer)
# Overflow is 108 bytes, reducing stack by 128 bytes provides minimal margin
CONFIG_MAIN_STACK_SIZE=896
