sample:
  name: Bluetooth Central Notify Receive
tests:
  sample.bluetooth.central_notify_receive:
    harness: bluetooth
    platform_allow:
      - qemu_cortex_m3
      - qemu_x86
      - nrf52_bsim
    tags: bluetooth
    integration_platforms:
      - qemu_cortex_m3
