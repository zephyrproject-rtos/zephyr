sample:
  name: Bluetooth Group Chat - Mixer
  description: LE Broadcast Sink, BIG with 2 BIS, Rx all BISes
tests:
  sample.bluetooth.iso_group_chat.mixer:
    build_only: true  # Ensures only the build process is executed, no functional tests
    platform_allow:
      - qemu_cortex_m3
      - qemu_x86
      - nrf52_bsim
      - nrf52840dk/nrf52840
    integration_platforms:
      - nrf52840dk/nrf52840
    tags:
      - bluetooth 
