sample:
  name: Bluetooth Group Chat - Secondary
  description: LE Broadcast Sink, BIG with 2 BIS, Rx on BIS1, Tx on BIS2
tests:
  sample.bluetooth.iso_group_chat.mixer.secondary:
    build_only: true  # Ensures only the build process is executed, no functional tests
    platform_allow:
      - qemu_cortex_m3
      - qemu_x86
      - nrf52_bsim
      - nrf52840dk/nrf52840
    integration_platforms:
      - nrf52840dk/nrf52840
    tags:
      - bluetooth 
