sample:
  description: syscall performances sample
  name: syscall performances
common:
  tags: userspace
  harness: console
  harness_config:
    type: one_line
    regex:
      - "User thread"
tests:
  sample.syscall_performances:
    platform_key:
      - arch
    depends_on_anyof:
      - mmu
      - mpu
    arch_allow: riscv
