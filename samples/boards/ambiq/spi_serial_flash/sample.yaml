sample:
  name: SPI PSRAM Module Sample
tests:
  sample.drivers.spi.apm_aps6404l:
    tags:
      - drivers
      - psram
    harness: psram
    depends_on:
      - gpio
      - spi
    filter: dt_alias_exists("spi_psram")
