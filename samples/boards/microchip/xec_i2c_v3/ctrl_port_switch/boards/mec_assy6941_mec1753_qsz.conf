# Uncomment to enable driver state capture to facilitate debug
# of the driver. Each instance of the driver contains an array
# of state transitions per transfer.
#CONFIG_I2C_MCHP_XEC_V3_BM_STATE_CAPTURE=y
