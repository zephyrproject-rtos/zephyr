sample:
  description: SensorTile.box board testing
  name: SensorTile.box test
tests:
  sample.board.sensortile_box.sensors:
    harness: sensor
    platform_whitelist: sensortile_box
    tags: sensors
    pre-filter: subsys_dt_compat_enabled("i2c") and subsys_dt_compat_enabled("spi") and subsys_dt_compat_enabled("gpio")
