CONFIG_PM=y
CONFIG_PM_DEVICE=y
CONFIG_FLASH=y
CONFIG_MAIN_STACK_SIZE=4096
# The flash TURN_ON restore re-runs the SFDP probe in the idle-thread context
# at PM3 wake; it needs a deeper idle stack than the default.
CONFIG_IDLE_STACK_SIZE=2048
