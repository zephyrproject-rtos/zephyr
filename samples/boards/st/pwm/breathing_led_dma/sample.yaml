sample:
  name: PWM with DMA sample
tests:
  sample.boards.stm32.uart.circular_dma:
    integration_platforms:
      - nucleo_f412zg
    tags:
      - pwm
      - dma
    filter: dt_chosen_enabled("zephyr,shell-uart") and CONFIG_SOC_FAMILY_STM32
    harness: keyboard
