

#get_filename_component(config_base "${CONFIG_MBEDTLS_CFG_FILE}" ABSOLUTE)
#get_filename_component(config_user_def "${CONFIG_MBEDTLS_USER_CONFIG_FILE}" ABSOLUTE)

#message("----${config_base}------config_user_def: ${config_user_def}")

#include_directories(${config_base}  ${config_user_def})

