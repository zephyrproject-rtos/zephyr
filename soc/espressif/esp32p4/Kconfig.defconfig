# Copyright (c) 2026 Espressif Systems (Shanghai) Co., Ltd.
# SPDX-License-Identifier: Apache-2.0

if SOC_ESP32P4_HPCORE

config FPU
	default y

# Multi-level/INTMUX model. With RISCV_RESERVED_IRQ_ISR_TABLES_OFFSET = 16, CPU
# lines occupy table slots 16..47, so the source window starts at 48. The
# aggregator spans 4 status words (sources 0..127); the highest source used on
# this SoC is 111, so NUM_IRQS = 48 + 128.
config NUM_IRQS
	default 176

config RISCV_MCAUSE_EXCEPTION_MASK
	default 0xFFF

config RISCV_RESERVED_IRQ_ISR_TABLES_OFFSET
	default 16

config MULTI_LEVEL_INTERRUPTS
	default y

config 2ND_LEVEL_INTERRUPTS
	default y

config 1ST_LEVEL_INTERRUPT_BITS
	default 8

config 2ND_LEVEL_INTERRUPT_BITS
	default 8

config 2ND_LVL_ISR_TBL_OFFSET
	default 48

config 2ND_LVL_INTR_00_OFFSET
	default 48

config MAX_IRQ_PER_2ND_LEVEL_AGGREGATOR
	default 128

config NUM_2ND_LEVEL_AGGREGATORS
	default 1

config RISCV_SOC_CONTEXT_SAVE
	default y

config RISCV_ALWAYS_SWITCH_THROUGH_ECALL
	default y if MULTITHREADING

config PMP_SLOTS
	default 16

config PMP_NO_LOCK_GLOBAL
	default y

configdefault CACHE_MANAGEMENT
	default y

choice CACHE_TYPE
	default EXTERNAL_CACHE if CACHE_MANAGEMENT
endchoice

config MAIN_STACK_SIZE
	default 2048

config ISR_STACK_SIZE
	default 4096

endif # SOC_ESP32P4_HPCORE

if SOC_ESP32P4_LPCORE

config GEN_ISR_TABLES
	default n

config SYS_CLOCK_EXISTS
	default n

config KERNEL_MEM_POOL
	default n

config COMMON_LIBC_MALLOC_ARENA_SIZE
	default 0

config MULTITHREADING
	default n

config NUM_PREEMPT_PRIORITIES
	default 0

endif
